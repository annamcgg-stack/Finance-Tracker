import type { SetupChoice } from "@/lib/types";

export const SETUP_CHOICE_LABELS: Record<SetupChoice, string> = {
  individual: "Individual",
  create_household: "Create household",
  join_household: "Join household",
};
